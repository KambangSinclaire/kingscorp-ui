
interface paginatedData {
    fields:[]
}
const paginateDataFunction = (paginated:object | string , index:number, paginatedData:any[], memo = {}) => {
    try {
        let data:any;
        //  paginatedData.filter()
    } catch (error) {
        console.log(error);
    }
}
